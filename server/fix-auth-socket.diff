diff --git a/src/store/useAuthStore.ts b/src/store/useAuthStore.ts
index 1234567..89abcde 100644
--- a/src/store/useAuthStore.ts
+++ b/src/store/useAuthStore.ts
@@
-    // Store token in localStorage and secure cookie
-    if (res.token) {
-      localStorage.setItem("token", res.token);
-      setSecureCookie("token", res.token, 30); // 30 days
-    }
+    // Store token in localStorage dan cookie dengan nama "at"
+    if (res.token) {
+      localStorage.setItem("at", res.token);
+      setSecureCookie("at", res.token, 30); // 30 days
+    }
@@
-    // Store token in localStorage and secure cookie
-    if (res.token) {
-      localStorage.setItem("token", res.token);
-      setSecureCookie("token", res.token, 30); // 30 days
-    }
+    if (res.token) {
+      localStorage.setItem("at", res.token);
+      setSecureCookie("at", res.token, 30); // 30 days
+    }
@@
-    // Clear token from localStorage and cookies
-    localStorage.removeItem("token");
-    eraseCookie("token");
+    // Clear token dari localStorage dan cookie
+    localStorage.removeItem("at");
+    eraseCookie("at");
diff --git a/src/lib/socket.ts b/src/lib/socket.ts
index 1234567..89abcde 100644
--- a/src/lib/socket.ts
+++ b/src/lib/socket.ts
@@
-// Ambil token dari cookie atau localStorage
-function getToken(): string | null {
-  return getCookie("at"); // 🔥 hanya gunakan "at"
-}
+// Ambil token dari cookie atau fallback localStorage
+function getToken(): string | null {
+  return getCookie("at") || localStorage.getItem("at");
+}
diff --git a/src/routes/auth.ts b/src/routes/auth.ts
index 1234567..89abcde 100644
--- a/src/routes/auth.ts
+++ b/src/routes/auth.ts
@@
-      res.json({
-        user: {
-          id: user.id,
-          email,
-          username,
-          name,
-          avatarUrl: user.avatarUrl,
-        },
-      });
+      res.json({
+        user: {
+          id: user.id,
+          email,
+          username,
+          name,
+          avatarUrl: user.avatarUrl,
+        },
+        token: tokens.access, // ✅ kirim access token ke frontend
+      });
@@
-      res.json({
-        user: {
-          id: user.id,
-          email: user.email,
-          username: user.username,
-          name: user.name,
-          avatarUrl: user.avatarUrl,
-        },
-      });
+      res.json({
+        user: {
+          id: user.id,
+          email: user.email,
+          username: user.username,
+          name: user.name,
+          avatarUrl: user.avatarUrl,
+        },
+        token: tokens.access, // ✅ kirim access token ke frontend
+      });
